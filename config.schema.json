{
  "pluginAlias": "OhmConnect",
  "pluginType": "accessory",
  "singular": false,
  "headerDisplay": "You can find your OhmConnect ID on the [OhmConnect API settings page](https://login.ohmconnect.com/api/v2/settings). It’s the string after the last / in the URL, e.g., for the URL https://login.ohmconnect.com/verify-ohm-hour/AbCd1e your ID is AbCd1e.",
  "schema": {
      "type": "object",
      "properties": {
        "name": {
          "title": "OhmConnect Location Name",
          "type": "string",
          "required": true,
          "default": "OhmConnect"
        },
        "ohmConnectId": {
          "title": "OhmConnect ID",
          "type": "string",
          "required": true,
          "placeholder": "AbCd1e"
        },
        "refreshMinutes": {
          "title": "Polling Interval in minutes",
          "type": "number",
          "required": true,
          "default": 5
        }
      }
  }
}